/** @type {import('ts-jest').JestConfigWithTsJest} */
export default {
	preset: "ts-jest",
	testEnvironment: "node",
	verbose: true, //note: it should be kept true for displaying the details of tests which is running in the cli.
};
